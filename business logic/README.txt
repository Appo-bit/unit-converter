Нужно скачать фреймверк Newtonsoft.Json (диспетчер пакетов NuGet -> в поиске "Newtonsoft.Json ")

В Json-файле находятся (будут находится) все возможные величины для конвертации. Поля:
1) index - шоб було так сказамц
2) name - что за величина
3) isCI - коэффициент соотношения к системе СИ.
 
Для конвертации нужно создать экземпляр класса ConverterWithJson и передать путь к Json-файлу
В этом экземпляре задаются такие значения:
1) CategoryOfValue - категория величин
2) IndexForVolumeFrom - айдишник начальной величины
3) IndexForVolumeTo - айдишник "конвертированной" величины

после, вызывается метод ConvertVolume(volume), где volume - значение начальной величины.

Прикрепил Main, где все тестилось.
Логов, конфига, обработки ошибок пока не делал.

Json-файл постепенно буду заполнять, могу сделать таблицу соотношения айди - величина, категории - ее название.

